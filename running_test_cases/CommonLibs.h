#ifndef COMMON_LIBS_H
#define COMMON_LIBS_H

using namespace std;
#include <iostream> // for standard I/O operations
#include <fstream>  // for file stream operations
#include <cmath>    // for mathematical functions
#include <vector>   // for using the vector container
#include <string>   // for string manipulation
#include <map>      // for using map containers
#include <unordered_map> // for using unordered map containers
#include <set>      // for using set containers
#include <unordered_set> // for using unordered set containers
#include <algorithm>// for algorithms like sort, reverse
#include <numeric>  // for numeric operations
#include <thread>   // for using threads
#include <mutex>    // for using mutexes for synchronization
#include <condition_variable> // for using condition variables
#include <queue>    // for using queue container
#include <deque>    // for using deque container
#include <list>     // for using list container
#include <array>    // for using array container
#include <functional> // for function objects
#include <memory>   // for smart pointers
#include <cstring>

#endif // COMMON_LIBS_H
